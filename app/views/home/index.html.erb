<%if user_signed_in? %>

  <div class="page_title">買い物リスト</div>

  <div class= "feed">
   <%= render 'notes/notes_feed' %>
  </div>
  <div class= "forms">
   <%= render 'notes/notes_form' %>
  </div>

<% else %>

<p>買い物メモアプリへようこそ</p>

<h2>まずは<%= link_to "コチラ",signup_path %>から新規登録！</h2>

<% end %>
