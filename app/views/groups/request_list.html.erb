<p>グループリクエスト一覧</p>

  <% if @user.group_members.present? %>
    <% @user.group_members.each do |member| %>
      <% unless member.activated.present? %>
        <ul>
　　　　　　<li><img src= <%= member.group.image %> class= "image_icon" alt="/assets/default_group_icon.png"></li>
          
          <li><%= member.group.name %></li>

          <li>
            <%= form_with url:join_group_path(member.group_id) do |f| %>

            <%= f.hidden_field :user_id, :value => @user.id %>

            <%= f.submit "グループに入る",class: "btn btn-primary" %>
          </li>

          <% end %>
        </ul>
      <% else %>
        <p>現在リクエストはありません</p>
        <% break %>

      <% end %>
    <% end %>
  <% else %>

   <p>現在リクエストはありません</p>

  <% end %>
