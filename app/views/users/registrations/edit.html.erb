<h2>ユーザー情報変更 </h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label :ユーザー名 %><br />
    <%= f.text_field :name,autocomplete: "email" %>
  </div>

  <div class="field">
    <%= f.label :メールアドレス %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div class="field">
    <%= f.label :新規パスワード %> <i>(変更を希望しない場合は入力しないでください)</i><br />
    <%= f.password_field :password, autocomplete: "new-password" %>
    <% if @minimum_password_length %>
      <br />
      <em><%= @minimum_password_length %> 文字以上で入力してください</em>
    <% end %>
  </div>

  <div class="field">
    <%= f.label :新規パスワード確認用 %><br />
    <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
  </div>

  <div class="field">
    <%= f.label :現在のパスワード %> <i>(ユーザー認証のため必ず入力してください)</i><br />
    <%= f.password_field :current_password, autocompletSe: "current-password" %>
  </div>

  <div class="field">
    <%= f.label :グループアイコン, class:'image_form' do %>
    <label>
      <div class="btn btn_deault">ユーザーアイコンを選択</div>
      <%= f.file_field :image, class: "image_form_title" %>
    </label>
    <% end %>
  </div>

  <div class="action"><br/>
    <%= f.submit "ユーザー情報変更",class:"btn btn_primary" %>
  </div><br  />
<% end %>

<h4>アカウント削除</h4>

<p>※一度削除したアカウントは元に戻せません<br/>
希望の方は下記ボタンをクリックしてください
<%= button_to "アカウント削除", registration_path(resource_name), data: { confirm: "本当に削除しますか?" }, method: :delete, class:"btn btn_warningS" %></p>
