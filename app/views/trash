<% @feed.each do |i| %>

  <%= i.user.name %>
  <div class="i_content">
    <%= check_box_tag 'i[id][]', i.id, checked = false, :id => "note_#{i.id}" %>
    <%= i.content %>
  </div>
  <div class="time"><%= i.created_at.to_s(:datetime) %></div>

<% end %>
